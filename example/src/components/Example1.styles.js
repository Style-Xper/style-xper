import { synth } from "../utils/synth";

export const styles = synth.createStyles({
  wrapper: {
    fontSize: "100px",
    background: "var(--secondary)",
    padding: "16px"
  },
  helloWorld: {
    color: "black",
    fontWeight: "700"
  },
});
